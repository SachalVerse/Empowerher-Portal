global:
  scrape_interval: 15s # How often to collect data

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node-exporter' # Monitors host VM CPU/RAM metrics
    static_configs:
      - targets: ['node-exporter:9100']